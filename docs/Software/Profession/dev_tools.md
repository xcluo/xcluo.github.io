---
title: "Dev-Tools"
---

### 网络Network

- **筛选器 Filter** 对网络结果进行筛选
- **保留日志 Preserve Log** 当网页跳转或刷新时，保留历史网络资源

### 监控 `javascript:void(0)` 点击事件

- F12 → 元素(Element) → 右上角事件监听器 → click
- 点击元素中 `href=javascript:void(0)` 部分
- 点击打开监听事件弹出的 .js 文件
- 搜索监听事件左侧的元素名（如a.forgetpwd则在.js文件中搜索 forgetpwd 关键字）
